# Pinned to exact versions — prevents the Keras quantization_config
# version-skew bug that caused the first Render deployment to crash.
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
tensorflow-cpu==2.16.1
keras==3.12.2
opencv-python-headless==4.10.0.84
numpy==1.26.4
pillow==10.3.0
